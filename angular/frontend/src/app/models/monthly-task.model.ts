export interface MonthlyTaskCreateModel {
    monthlyTaskName: string;
    dayOfMonth: number;
}

export interface MonthlyTaskModel {
    id: number;
    monthlyTaskName: string;
    dayOfMonth: number;
}
