export interface UserProfileEditModel {
    givenName: string;
    surname: string;
    email: string;
}

export interface UserProfileModel {
    id: number;
    givenName: string;
    surname: string;
    email: string;
    username: string;
}
